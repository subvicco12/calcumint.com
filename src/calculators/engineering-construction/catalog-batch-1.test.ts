import{describe,expect,it}from"vitest";import{concreteCalculator,squareFootageCalculator,paintCalculator,flooringCalculator,roofingCalculator,tileCalculator,drywallCalculator,lumberCalculator}from"./catalog-batch-1";import{runCalculator}from"../engine";
describe("engineering construction #343-350 certification boundaries",()=>{
it("accepts waste boundaries and zero roof pitch",()=>{expect(runCalculator(concreteCalculator,{lengthMeters:5,widthMeters:4,depthMeters:.1,wastePercent:0}).output.value).toBe(2);expect(runCalculator(flooringCalculator,{lengthMeters:5,widthMeters:4,wastePercent:100}).output.value).toBe(40);expect(runCalculator(roofingCalculator,{buildingLengthFeet:40,buildingWidthFeet:30,pitchRisePer12:0,wastePercent:0}).output.value).toBe(12)});
it("rejects nonpositive dimensions and invalid waste",()=>{expect(()=>runCalculator(concreteCalculator,{lengthMeters:0,widthMeters:4,depthMeters:.1,wastePercent:10})).toThrow();expect(()=>runCalculator(flooringCalculator,{lengthMeters:5,widthMeters:0,wastePercent:10})).toThrow();expect(()=>runCalculator(paintCalculator,{wallAreaSquareMeters:100,coats:2,coverageSquareMetersPerLiter:0,wastePercent:10})).toThrow();expect(()=>runCalculator(tileCalculator,{areaSquareMeters:20,tileLengthCm:0,tileWidthCm:50,wastePercent:10})).toThrow();expect(()=>runCalculator(drywallCalculator,{surfaceAreaSquareFeet:1000,sheetLengthFeet:8,sheetWidthFeet:0,wastePercent:10})).toThrow();expect(()=>runCalculator(lumberCalculator,{thicknessInches:0,widthInches:6,lengthFeet:10,quantity:10})).toThrow();expect(()=>runCalculator(concreteCalculator,{lengthMeters:5,widthMeters:4,depthMeters:.1,wastePercent:-.01})).toThrow();expect(()=>runCalculator(concreteCalculator,{lengthMeters:5,widthMeters:4,depthMeters:.1,wastePercent:100.01})).toThrow()});
it("rejects invalid integer quantities",()=>{expect(()=>runCalculator(squareFootageCalculator,{lengthFeet:12,widthFeet:10,quantity:0})).toThrow();expect(()=>runCalculator(squareFootageCalculator,{lengthFeet:12,widthFeet:10,quantity:1.5})).toThrow();expect(()=>runCalculator(paintCalculator,{wallAreaSquareMeters:100,coats:1.5,coverageSquareMetersPerLiter:10,wastePercent:10})).toThrow();expect(()=>runCalculator(lumberCalculator,{thicknessInches:2,widthInches:6,lengthFeet:10,quantity:1.5})).toThrow()});
it("rounds purchase counts upward",()=>{expect(runCalculator(tileCalculator,{areaSquareMeters:1,tileLengthCm:60,tileWidthCm:60,wastePercent:0}).output.value).toBe(3);expect(runCalculator(drywallCalculator,{surfaceAreaSquareFeet:33,sheetLengthFeet:8,sheetWidthFeet:4,wastePercent:0}).output.value).toBe(2)});
it("rejects negative pitch and non-finite overflow",()=>{expect(()=>runCalculator(roofingCalculator,{buildingLengthFeet:40,buildingWidthFeet:30,pitchRisePer12:-1,wastePercent:10})).toThrow();expect(()=>runCalculator(concreteCalculator,{lengthMeters:1e200,widthMeters:1e200,depthMeters:1e200,wastePercent:100})).toThrow()});
});