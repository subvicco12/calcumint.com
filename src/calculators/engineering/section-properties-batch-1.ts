import{z}from"zod";import type{CalculatorDefinition}from"../types";
type Out={value:number;steps:readonly string[]};const pos=z.number().finite().positive().max(1e50),nonneg=z.number().finite().min(0).max(1e50),finite=z.number().finite().min(-1e50).max(1e50);
const src={label:"OpenStax — Centroids and area properties context",url:"https://openstax.org/details/books/university-physics-volume-1",note:"Geometry/mechanics reference context; formulas are standard plane-area centroid relations."};
const checked=(v:number)=>{if(!Number.isFinite(v))throw new Error("Calculated result is outside the supported finite range");return v};const mk=<I>(id:string,slug:string,title:string,schema:z.ZodType<I>,calc:(x:I)=>number,expr:string,desc:string,example:I,expected:number)=>({id,slug,title,category:"engineering",version:1,riskClass:"standard",reviewStatus:"draft",inputSchema:schema,calculate:(x:I)=>{const value=checked(calc(x));return{value,steps:[title.replace(" Calculator","")+" = "+value]}},formulas:[{id,expression:expr,description:desc}],sources:[src],examples:[{label:"Worked example",input:example,expected:{value:expected,steps:[title.replace(" Calculator","")+" = "+expected]}}],goldenTests:[{label:"Worked example",input:example,expected:{value:expected}}]}) satisfies CalculatorDefinition<I,Out>;
export const compositeCentroid=mk("engineering.composite-area-centroid","composite-area-centroid-calculator","Two-Area Composite Centroid Calculator",z.object({area1M2:pos,centroid1M:finite,area2M2:pos,centroid2M:finite}),x=>(x.area1M2*x.centroid1M+x.area2M2*x.centroid2M)/(x.area1M2+x.area2M2),"ybar=(A1y1+A2y2)/(A1+A2)","Centroid coordinate of two positive component areas measured from a common datum.",{area1M2:2,centroid1M:1,area2M2:1,centroid2M:4},2);
export const triangleCentroid=mk("engineering.triangle-centroid-from-base","triangle-centroid-calculator","Triangle Centroid from Base Calculator",z.object({heightM:nonneg}),x=>x.heightM/3,"ybar=h/3","Centroid distance from a triangle base along its altitude.",{heightM:3},1);
export const semicircleArea=mk("engineering.semicircle-area","semicircle-area-calculator","Semicircle Area Calculator",z.object({radiusM:nonneg}),x=>Math.PI*x.radiusM*x.radiusM/2,"A=pi*r^2/2","Area of a semicircle.",{radiusM:2},2*Math.PI);
export const semicircleCentroid=mk("engineering.semicircle-centroid-from-diameter","semicircle-centroid-calculator","Semicircle Centroid from Diameter Calculator",z.object({radiusM:nonneg}),x=>4*x.radiusM/(3*Math.PI),"ybar=4r/(3pi)","Centroid distance of a semicircular area from its diameter along the symmetry axis.",{radiusM:3},4/Math.PI);
const annulus=z.object({outerRadiusM:pos,innerRadiusM:nonneg}).refine(x=>x.innerRadiusM<x.outerRadiusM,{message:"Inner radius must be smaller than outer radius"});
export const annulusArea=mk("engineering.annulus-area","annulus-area-calculator","Annulus Area Calculator",annulus,x=>Math.PI*(x.outerRadiusM*x.outerRadiusM-x.innerRadiusM*x.innerRadiusM),"A=pi(R^2-r^2)","Area between concentric outer and inner circles.",{outerRadiusM:2,innerRadiusM:1},3*Math.PI);
export const sectionPropertiesBatch1Definitions=[compositeCentroid,triangleCentroid,semicircleArea,semicircleCentroid,annulusArea]as const;
