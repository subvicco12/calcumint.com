import {describe,expect,it} from "vitest";
import {breakEvenPriceForTargetUnits,breakEvenScenarios,breakEvenSensitivity,compoundInterestPrincipalForTarget,compoundInterestScenarios,compoundInterestSensitivity,mortgageRateSensitivity,mortgageScenarios,loanRateSensitivity,loanScenarios,loanSchedule,sipGoal,sipScenarios,sipSensitivity} from "./analysis";
import {runCalculator} from "../engine";
import {loanAnalysisCalculator,maxPrincipalForPayment} from "../finance/loan-analysis";
import {loanPaymentCalculator} from "../finance/loan-payment";
import {compoundInterestCalculator} from "../finance/compound-interest";
import {sipCalculator} from "../finance/sip";
import {breakEvenCalculator} from "../business/break-even";
describe("final analysis engine",()=>{
 it("amortization schedule reaches zero",()=>{const rows=loanSchedule({principal:100000,annualRatePercent:6,termMonths:360});expect(rows.at(-1)?.values.balance).toBeCloseTo(0,2);expect(rows.at(-1)?.period).toBe(360)});
 it("loan presentation schedule reconciles with certified amortization checkpoints",()=>{const input={principal:100000,annualRatePercent:6,termMonths:360};const rows=loanSchedule(input);const certified=runCalculator(loanAnalysisCalculator,{...input,extraMonthlyPayment:0}).output.amortization;for(const row of rows){const source=certified.find(item=>item.month===row.period);expect(source).toBeDefined();expect(row.values.payment).toBe(source!.payment);expect(row.values.principal).toBe(source!.principal);expect(row.values.interest).toBe(source!.interest);expect(row.values.balance).toBe(source!.balance)}});
 it("loan sensitivity is monotonic with rate",()=>{const p=loanRateSensitivity({principal:100000,annualRatePercent:6,termMonths:360});expect(p[0].value).toBeLessThan(p.at(-1)!.value)});
 it("loan scenarios preserve the base case",()=>{const s=loanScenarios({principal:100000,annualRatePercent:6,termMonths:360});expect(s[1].delta).toBe(0)});
 it("SIP goal solver reconciles with target",()=>{expect(sipGoal(2065520.2,{annualReturnPercent:10,termMonths:120,contributionTiming:"beginning"})).toBeCloseTo(10000,1)});
 it("mortgage reverse solver reconciles target payment with the certified loan engine",()=>{const targetPayment=2000;const annualRatePercent=6.5;const termMonths=360;const principal=maxPrincipalForPayment(targetPayment,annualRatePercent,termMonths);const output=runCalculator(loanPaymentCalculator,{principal,annualRatePercent,termMonths}).output;expect(output.monthlyPayment).toBeCloseTo(targetPayment,8)});
 it("mortgage rate sensitivity reconciles every point with the certified loan engine",()=>{const input={principal:320000,annualRatePercent:6.5,termMonths:360};const points=mortgageRateSensitivity(input);for(const point of points){expect(point.value).toBe(runCalculator(loanPaymentCalculator,{...input,annualRatePercent:point.input}).output.monthlyPayment)}});
 it("mortgage scenarios reconcile to the certified loan engine and base delta",()=>{const input={principal:320000,annualRatePercent:6.5,termMonths:360};const scenarios=mortgageScenarios(input);expect(scenarios.find(item=>item.label==="0% rate")?.delta).toBe(0);for(const scenario of scenarios){const delta=Number.parseFloat(scenario.label);const rate=Math.max(0,input.annualRatePercent+delta);expect(scenario.value).toBe(runCalculator(loanPaymentCalculator,{...input,annualRatePercent:rate}).output.monthlyPayment)}});
 it("SIP scenarios reconcile every point with the certified engine",()=>{const input={monthlyContribution:10000,annualReturnPercent:10,termMonths:120,contributionTiming:"beginning" as const};const scenarios=sipScenarios(input);expect(scenarios.find(item=>item.label==="0% return")?.delta).toBe(0);for(const scenario of scenarios){const delta=Number.parseFloat(scenario.label);const rate=Math.max(-99,input.annualReturnPercent+delta);expect(scenario.value).toBe(runCalculator(sipCalculator,{...input,annualReturnPercent:rate}).output.futureValue)}});
 it("compound scenarios reconcile every point with the certified engine",()=>{const input={principal:10000,annualRatePercent:5,years:10,compoundsPerYear:12};const scenarios=compoundInterestScenarios(input);expect(scenarios.find(item=>item.label==="0% rate")?.delta).toBe(0);for(const scenario of scenarios){const delta=Number.parseFloat(scenario.label);const rate=Math.max(-99,input.annualRatePercent+delta);expect(scenario.value).toBe(runCalculator(compoundInterestCalculator,{...input,annualRatePercent:rate}).output.futureValue)}});
 it("compound principal goal solver reconciles with the certified engine",()=>{const target=25000;const input={annualRatePercent:5,years:10,compoundsPerYear:12};const principal=compoundInterestPrincipalForTarget(target,input);const output=runCalculator(compoundInterestCalculator,{principal,...input}).output;expect(output.futureValue).toBeCloseTo(target,8)});
 it("compound sensitivity reconciles every point with the certified engine",()=>{const input={principal:10000,annualRatePercent:5,years:10,compoundsPerYear:12};const points=compoundInterestSensitivity(input);for(const point of points){const expected=runCalculator(compoundInterestCalculator,{...input,annualRatePercent:point.input}).output.futureValue;expect(point.value).toBe(expected)}expect(points[0].value).toBeLessThan(points.at(-1)!.value)});
 it("break-even reverse solver reconciles target units with the deterministic engine",()=>{const input={fixedCosts:10000,variableCostPerUnit:30};const targetUnits=400;const pricePerUnit=breakEvenPriceForTargetUnits(targetUnits,input);const output=runCalculator(breakEvenCalculator,{...input,pricePerUnit}).output;expect(pricePerUnit).toBe(55);expect(output.breakEvenUnits).toBeCloseTo(targetUnits,2)});
 it("break-even scenarios reconcile with the deterministic engine",()=>{const input={fixedCosts:10000,pricePerUnit:50,variableCostPerUnit:30};const scenarios=breakEvenScenarios(input);expect(scenarios.find(item=>item.label==="0% price")?.delta).toBe(0);for(const scenario of scenarios){const percent=Number.parseFloat(scenario.label);const price=input.pricePerUnit*(1+percent/100);expect(scenario.value).toBe(runCalculator(breakEvenCalculator,{...input,pricePerUnit:price}).output.breakEvenUnits)}});
 it("break-even price sensitivity reconciles and falls as price rises",()=>{const input={fixedCosts:10000,pricePerUnit:50,variableCostPerUnit:30};const points=breakEvenSensitivity(input);for(const point of points){expect(point.value).toBe(runCalculator(breakEvenCalculator,{...input,pricePerUnit:point.input}).output.breakEvenUnits)}expect(points[0].value).toBeGreaterThan(points.at(-1)!.value)});
 it("SIP sensitivity increases with modeled return",()=>{const p=sipSensitivity({monthlyContribution:10000,annualReturnPercent:10,termMonths:120,contributionTiming:"beginning"});expect(p[0].value).toBeLessThan(p.at(-1)!.value)});
});