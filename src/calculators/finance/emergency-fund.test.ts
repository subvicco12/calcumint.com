import { describe,expect,it } from "vitest";import { runCalculator } from "../engine";import { calculateEmergencyFund,emergencyFundCalculator } from "./emergency-fund";
describe("emergency fund",()=>{it("calculates target gap coverage and build contribution",()=>{expect(calculateEmergencyFund({monthlyEssentialExpenses:2000,targetMonths:6,currentEmergencySavings:3000,buildMonths:9})).toEqual({targetFund:12000,currentSavings:3000,fundingGap:9000,coverageMonths:1.5,requiredMonthlySavings:1000,targetReached:false});});it("does not report a negative gap",()=>{const r=calculateEmergencyFund({monthlyEssentialExpenses:1000,targetMonths:3,currentEmergencySavings:5000});expect(r.fundingGap).toBe(0);expect(r.targetReached).toBe(true);});it("handles zero essential expenses without an infinite coverage ratio",()=>{const r=calculateEmergencyFund({monthlyEssentialExpenses:0,targetMonths:6,currentEmergencySavings:100});expect(r.coverageMonths).toBeNull();expect(r.targetReached).toBe(true);});it("validates through the common engine",()=>{expect(()=>runCalculator(emergencyFundCalculator,{monthlyEssentialExpenses:1000,targetMonths:0,currentEmergencySavings:0})).toThrow();const r=runCalculator(emergencyFundCalculator,{monthlyEssentialExpenses:1000,targetMonths:3,currentEmergencySavings:0,buildMonths:6});expect(r.calculatorId).toBe("finance.emergency-fund");expect(r.output.requiredMonthlySavings).toBe(500);});it("rejects unsafe direct model inputs",()=>{expect(()=>calculateEmergencyFund({monthlyEssentialExpenses:-1,targetMonths:6,currentEmergencySavings:0})).toThrow();expect(()=>calculateEmergencyFund({monthlyEssentialExpenses:1000,targetMonths:Number.NaN,currentEmergencySavings:0})).toThrow();expect(()=>calculateEmergencyFund({monthlyEssentialExpenses:1000,targetMonths:6,currentEmergencySavings:0,buildMonths:1.5})).toThrow();expect(()=>calculateEmergencyFund({monthlyEssentialExpenses:1000,targetMonths:37,currentEmergencySavings:0})).toThrow();expect(()=>calculateEmergencyFund({monthlyEssentialExpenses:1000,targetMonths:6,currentEmergencySavings:0,buildMonths:601})).toThrow();});});