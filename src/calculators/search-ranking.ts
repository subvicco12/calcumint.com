export type RankableCalculatorSearchItem={title:string;description:string;keywords:readonly string[]};
export function tokenizeCalculatorQuery(value:string):string[]{return value.toLocaleLowerCase().split(/[^a-z0-9]+/).filter(token=>token.length>1)}
export function rankCalculatorSearchItems<T extends RankableCalculatorSearchItem>(items:readonly T[],query:string,limit:number):readonly T[]{
 if(!Number.isInteger(limit)||limit<1)return[];
 const queryTokens=[...new Set(tokenizeCalculatorQuery(query))];if(queryTokens.length===0)return[];const normalizedQuery=queryTokens.join(" ");
 return items.map(item=>{const title=tokenizeCalculatorQuery(item.title),keywords=tokenizeCalculatorQuery(item.keywords.join(" ")),description=tokenizeCalculatorQuery(item.description);let score=0;for(const token of queryTokens){if(title.includes(token))score+=5;if(keywords.includes(token))score+=3;if(description.includes(token))score+=1}const keywordCoverage=queryTokens.filter(token=>keywords.includes(token)).length;if(queryTokens.length>1&&keywordCoverage===queryTokens.length)score+=queryTokens.length*4;const normalizedKeywords=item.keywords.map(value=>tokenizeCalculatorQuery(value).join(" "));if(normalizedKeywords.includes(normalizedQuery))score+=12;return{item,score}}).filter(match=>match.score>0).sort((a,b)=>b.score-a.score||a.item.title.localeCompare(b.item.title)).slice(0,limit).map(match=>match.item);
}
