import{z}from"zod";import type{CalculatorDefinition}from"../types";
type Out={value:number;steps:readonly string[]};const pos=z.number().finite().positive().max(1e100),nonneg=z.number().finite().min(0).max(1e100),finite=z.number().finite().min(-1e100).max(1e100);
const src={label:"OpenStax University Physics",url:"https://openstax.org/details/books/university-physics-volume-1",note:"Reference for classical mechanics, rotation, oscillations, and collisions."};
const checked=(v:number)=>{if(!Number.isFinite(v))throw new Error("Calculated result is outside the supported finite range");return v};
const mk=<I>(id:string,slug:string,title:string,schema:z.ZodType<I>,calc:(x:I)=>number,expr:string,desc:string,example:I,expected:number)=>({id,slug,title,category:"science",version:1,riskClass:"standard",reviewStatus:"draft",inputSchema:schema,calculate:(x:I)=>{const value=checked(calc(x));return{value,steps:[title.replace(" Calculator","")+" = "+value]}},formulas:[{id,expression:expr,description:desc}],sources:[src],examples:[{label:"Worked example",input:example,expected:{value:expected,steps:[title.replace(" Calculator","")+" = "+expected]}}],goldenTests:[{label:"Worked example",input:example,expected:{value:expected}}]}) satisfies CalculatorDefinition<I,Out>;
export const restitutionCalculator=mk("physics.coefficient-of-restitution","coefficient-of-restitution-calculator","Coefficient of Restitution Calculator",z.object({approachSpeed:pos,recessionSpeed:nonneg}),x=>x.recessionSpeed/x.approachSpeed,"e = relative separation speed / relative approach speed","One-dimensional coefficient of restitution from relative speeds.",{approachSpeed:10,recessionSpeed:8},0.8);
export const solidDiskInertiaCalculator=mk("physics.solid-disk-inertia","moment-of-inertia-solid-disk-calculator","Moment of Inertia — Solid Disk Calculator",z.object({massKg:nonneg,radiusM:nonneg}),x=>0.5*x.massKg*x.radiusM*x.radiusM,"I = 1/2 m r^2","Moment of inertia of a uniform solid disk about its central symmetry axis.",{massKg:4,radiusM:2},8);
export const pendulumPeriodCalculator=mk("physics.simple-pendulum-period","simple-pendulum-period-calculator","Simple Pendulum Period Calculator",z.object({lengthM:pos,gravityMps2:pos}),x=>2*Math.PI*Math.sqrt(x.lengthM/x.gravityMps2),"T = 2*pi*sqrt(L/g)","Small-angle simple-pendulum period.",{lengthM:1,gravityMps2:9.80665},2.0064092925890407);
export const rotatingFrameCentrifugalCalculator=mk("physics.centrifugal-force","centrifugal-force-calculator","Centrifugal Force Calculator",z.object({massKg:nonneg,angularSpeedRadS:nonneg,radiusM:nonneg}),x=>x.massKg*x.angularSpeedRadS*x.angularSpeedRadS*x.radiusM,"F = m*omega^2*r","Magnitude of centrifugal pseudo-force in a uniformly rotating reference frame.",{massKg:2,angularSpeedRadS:3,radiusM:4},72);
export const radiusOfGyrationCalculator=mk("physics.radius-of-gyration","radius-of-gyration-calculator","Radius of Gyration Calculator",z.object({momentOfInertiaKgM2:nonneg,massKg:pos}),x=>Math.sqrt(x.momentOfInertiaKgM2/x.massKg),"k = sqrt(I/m)","Radius of gyration corresponding to moment of inertia I and total mass m.",{momentOfInertiaKgM2:8,massKg:2},2);
export const physicsMechanicsBatch4Definitions=[restitutionCalculator,solidDiskInertiaCalculator,pendulumPeriodCalculator,rotatingFrameCentrifugalCalculator,radiusOfGyrationCalculator]as const;
