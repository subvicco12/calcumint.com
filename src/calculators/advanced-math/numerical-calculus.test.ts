import{describe,expect,it}from"vitest";import{derivativeAt,integrate,limitAt}from"./numerical-calculus";describe("numerical calculus",()=>{it("differentiates smooth expressions",()=>{expect(derivativeAt("x^3",2)).toBeCloseTo(12,7);expect(derivativeAt("sin(x)",0)).toBeCloseTo(1,7)});it("integrates with adaptive Simpson refinement",()=>{expect(integrate("x^2",0,3).value).toBeCloseTo(9,9);expect(integrate("sin(x)",0,Math.PI).value).toBeCloseTo(2,8)});it("estimates finite limits",()=>{expect(limitAt("(x^2-1)/(x-1)",1).value).toBeCloseTo(2,3)});it("rejects unequal two-sided limits",()=>expect(()=>limitAt("abs(x)/x",0)).toThrow(/Two-sided limit/))});