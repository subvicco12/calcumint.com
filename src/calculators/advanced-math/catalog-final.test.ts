import{describe,expect,it}from"vitest";import{runCalculator}from"../engine";import{functionPlotter3dCalculator,matrixCalculator}from"./catalog-final";describe("advanced math final catalog batch",()=>{it("multiplies 2x2 matrices",()=>expect(runCalculator(matrixCalculator,{a11:1,a12:2,a21:3,a22:4,b11:5,b12:6,b21:7,b22:8,operation:"multiply"}).output.matrix).toEqual([[19,22],[43,50]]));it("adds matrices",()=>expect(runCalculator(matrixCalculator,{a11:1,a12:2,a21:3,a22:4,b11:5,b12:6,b21:7,b22:8,operation:"add"}).output.matrix).toEqual([[6,8],[10,12]]));it("samples a paraboloid",()=>{const o=runCalculator(functionPlotter3dCalculator,{expression:"x^2+y^2",minX:-2,maxX:2,minY:-2,maxY:2}).output;expect(o.points).toHaveLength(289);expect(o.minimum).toBe(0);expect(o.maximum).toBe(8)});it("omits undefined 3D samples instead of substituting values",()=>{const o=runCalculator(functionPlotter3dCalculator,{expression:"sqrt(x*y)",minX:-1,maxX:1,minY:-1,maxY:1}).output;expect(o.points.length).toBeLessThan(289);expect(o.points.every(p=>p.x*p.y>=0)).toBe(true)});});