import {describe,expect,it} from "vitest";import {runCalculator} from "../engine";import {rrefCalculator,sequenceCalculator,vectorCalculator} from "./catalog-batch-7";describe("advanced math catalog batch 7",()=>{it("reduces a 2x3 augmented matrix",()=>expect(runCalculator(rrefCalculator,{a:1,b:1,c:5,d:1,e:-1,f:1}).output.matrix).toEqual([[1,0,3],[0,1,2]]));it("computes 3D vector operations",()=>{const o=runCalculator(vectorCalculator,{ax:1,ay:0,az:0,bx:0,by:1,bz:0}).output;expect(o.dot).toBe(0);expect(o.cross).toEqual({x:0,y:0,z:1});expect(o.angleRadians).toBeCloseTo(Math.PI/2)});it("computes an arithmetic sequence",()=>expect(runCalculator(sequenceCalculator,{first:1,difference:2,n:10}).output).toMatchObject({nth:19,sum:100}))});