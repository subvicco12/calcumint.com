import {describe,expect,it} from "vitest";import {runCalculator} from "../engine";import {equationSolverCalculator,polynomialCalculator,rootFinderCalculator} from "./catalog-batch-8";describe("advanced math catalog batch 8",()=>{it("dispatches linear and quadratic equations",()=>{expect(runCalculator(equationSolverCalculator,{a:0,b:2,c:-8}).output.solutions).toEqual(["4"]);expect(runCalculator(equationSolverCalculator,{a:1,b:-5,c:6}).output.solutions).toEqual(["3","2"])});it("finds three cubic real roots",()=>{const o=runCalculator(polynomialCalculator,{a:1,b:-6,c:11,d:-6}).output;expect(o.roots).toHaveLength(3);expect(o.roots[0]).toBeCloseTo(1);expect(o.roots[1]).toBeCloseTo(2);expect(o.roots[2]).toBeCloseTo(3)});it("bisects a bracketed root",()=>{const o=runCalculator(rootFinderCalculator,{a:0,b:1,c:0,d:-2,left:1,right:2}).output;expect(o.root).toBeCloseTo(Math.SQRT2,10);expect(Math.abs(o.residual)).toBeLessThan(1e-10)});it("rejects a non-bracketing interval",()=>expect(()=>runCalculator(rootFinderCalculator,{a:0,b:1,c:0,d:1,left:1,right:2})).toThrow())});