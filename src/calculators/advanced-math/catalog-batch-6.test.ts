import {describe,expect,it} from "vitest";import {runCalculator} from "../engine";import {complexNumberCalculator,matrixInverseCalculator,systemOfEquationsCalculator} from "./catalog-batch-6";
describe("advanced math catalog batch 6",()=>{it("solves a unique 2x2 system",()=>{const o=runCalculator(systemOfEquationsCalculator,{a:1,b:1,c:5,d:1,e:-1,f:1}).output;expect(o.x).toBe(3);expect(o.y).toBe(2);expect(()=>runCalculator(systemOfEquationsCalculator,{a:1,b:1,c:2,d:2,e:2,f:4})).toThrow()});it("inverts a nonsingular 2x2 matrix",()=>{const o=runCalculator(matrixInverseCalculator,{a:4,b:7,c:2,d:6}).output;expect(o).toMatchObject({m11:.6,m12:-.7,m21:-.2,m22:.4,determinant:10});expect(()=>runCalculator(matrixInverseCalculator,{a:1,b:2,c:2,d:4})).toThrow()});it("performs complex arithmetic",()=>{const o=runCalculator(complexNumberCalculator,{a:1,b:2,c:3,d:4,operation:"multiply"}).output;expect(o.real).toBe(-5);expect(o.imaginary).toBe(10);expect(()=>runCalculator(complexNumberCalculator,{a:1,b:2,c:0,d:0,operation:"divide"})).toThrow()})});