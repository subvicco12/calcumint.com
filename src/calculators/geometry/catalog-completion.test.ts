import{describe,expect,it}from"vitest";import{runCalculator}from"../engine";import{triangleCalculator,rightTriangleCalculator,surfaceAreaCalculator,slopeCalculator,distanceCalculator}from"./catalog-completion";describe("geometry catalog completion",()=>{it("solves a 3-4-5 triangle",()=>{const o=runCalculator(triangleCalculator,{a:3,b:4,c:5}).output;expect(o.area).toBe(6);expect(o.perimeter).toBe(12)});it("solves right triangle",()=>expect(runCalculator(rightTriangleCalculator,{a:3,b:4}).output.area).toBe(6));it("computes prism surface",()=>expect(runCalculator(surfaceAreaCalculator,{length:2,width:3,height:4}).output.surfaceArea).toBe(52));it("computes slope",()=>expect(runCalculator(slopeCalculator,{x1:1,y1:2,x2:3,y2:6}).output.value).toBe(2));it("computes distance",()=>expect(runCalculator(distanceCalculator,{x1:0,y1:0,x2:3,y2:4}).output.value).toBe(5));});