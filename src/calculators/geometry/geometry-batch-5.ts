import{z}from"zod";import type{CalculatorDefinition}from"../types";
type Out={value:number;steps:readonly string[]};const pos=z.number().finite().positive().max(1e50),nonneg=z.number().finite().min(0).max(1e50);
const src={label:"OpenStax — Plane geometry reference context",url:"https://openstax.org/details/books/prealgebra-2e",note:"Geometry reference context for trapezoid and parallelogram relations."};
const checked=(v:number)=>{if(!Number.isFinite(v))throw new Error("Calculated result is outside the supported finite range");return v};const mk=<I>(id:string,slug:string,title:string,schema:z.ZodType<I>,calc:(x:I)=>number,expr:string,desc:string,example:I,expected:number)=>({id,slug,title,category:"geometry",version:1,riskClass:"standard",reviewStatus:"draft",inputSchema:schema,calculate:(x:I)=>{const value=checked(calc(x));return{value,steps:[title.replace(" Calculator","")+" = "+value]}},formulas:[{id,expression:expr,description:desc}],sources:[src],examples:[{label:"Worked example",input:example,expected:{value:expected,steps:[title.replace(" Calculator","")+" = "+expected]}}],goldenTests:[{label:"Worked example",input:example,expected:{value:expected}}]}) satisfies CalculatorDefinition<I,Out>;
const trap=z.object({base1M:pos,base2M:pos,heightM:pos});
export const trapezoidCentroid=mk("geometry.trapezoid-centroid","trapezoid-centroid-calculator","Trapezoid Centroid from Bases Calculator",trap,x=>x.heightM*(x.base1M+2*x.base2M)/(3*(x.base1M+x.base2M)),"ybar=h(b1+2b2)/(3(b1+b2))","Centroid distance from base 1 for a trapezoid with parallel bases b1 and b2 and perpendicular height h.",{base1M:2,base2M:4,heightM:3},1.6666666666666667);
export const trapezoidMidsegment=mk("geometry.trapezoid-midsegment","trapezoid-midsegment-calculator","Trapezoid Midsegment Calculator",z.object({base1M:pos,base2M:pos}),x=>(x.base1M+x.base2M)/2,"m=(b1+b2)/2","Length of the segment joining the midpoints of the trapezoid's legs.",{base1M:2,base2M:4},3);
export const parallelogramDiagonal=mk("geometry.parallelogram-diagonal","parallelogram-diagonal-calculator","Parallelogram Diagonal Calculator",z.object({side1M:pos,side2M:pos,includedAngleRad:z.number().finite().gt(0).lt(Math.PI)}),x=>Math.sqrt(x.side1M*x.side1M+x.side2M*x.side2M-2*x.side1M*x.side2M*Math.cos(x.includedAngleRad)),"d=sqrt(a^2+b^2-2ab cos(theta))","One parallelogram diagonal from two side lengths and their included angle.",{side1M:3,side2M:4,includedAngleRad:Math.PI/2},5);
export const parallelogramHeight=mk("geometry.parallelogram-height","parallelogram-height-calculator","Parallelogram Height Calculator",z.object({sideM:pos,includedAngleRad:z.number().finite().gt(0).lt(Math.PI)}),x=>x.sideM*Math.sin(x.includedAngleRad),"h=b sin(theta)","Perpendicular height associated with a side and included angle.",{sideM:4,includedAngleRad:Math.PI/6},2);
export const rhombusSideFromDiagonals=mk("geometry.rhombus-side-from-diagonals","rhombus-side-from-diagonals-calculator","Rhombus Side from Diagonals Calculator",z.object({diagonal1M:pos,diagonal2M:pos}),x=>0.5*Math.sqrt(x.diagonal1M*x.diagonal1M+x.diagonal2M*x.diagonal2M),"s=0.5sqrt(d1^2+d2^2)","Side length of a rhombus from its perpendicular diagonals.",{diagonal1M:6,diagonal2M:8},5);
export const geometryBatch5Definitions=[trapezoidCentroid,trapezoidMidsegment,parallelogramDiagonal,parallelogramHeight,rhombusSideFromDiagonals]as const;
