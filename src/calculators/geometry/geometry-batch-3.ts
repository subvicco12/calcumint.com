import{z}from"zod";import type{CalculatorDefinition}from"../types";
type Out={value:number;steps:readonly string[]};const pos=z.number().finite().positive().max(1e50),nonneg=z.number().finite().min(0).max(1e50),n=z.number().int().min(3).max(1000000);
const src={label:"OpenStax — Plane geometry reference context",url:"https://openstax.org/details/books/prealgebra-2e",note:"Geometry reference context for regular-polygon and annular-sector relations."};
const checked=(v:number)=>{if(!Number.isFinite(v))throw new Error("Calculated result is outside the supported finite range");return v};const mk=<I>(id:string,slug:string,title:string,schema:z.ZodType<I>,calc:(x:I)=>number,expr:string,desc:string,example:I,expected:number)=>({id,slug,title,category:"geometry",version:1,riskClass:"standard",reviewStatus:"draft",inputSchema:schema,calculate:(x:I)=>{const value=checked(calc(x));return{value,steps:[title.replace(" Calculator","")+" = "+value]}},formulas:[{id,expression:expr,description:desc}],sources:[src],examples:[{label:"Worked example",input:example,expected:{value:expected,steps:[title.replace(" Calculator","")+" = "+expected]}}],goldenTests:[{label:"Worked example",input:example,expected:{value:expected}}]}) satisfies CalculatorDefinition<I,Out>;
export const regularPolygonExteriorAngle=mk("geometry.regular-polygon-exterior-angle","regular-polygon-exterior-angle-calculator","Regular Polygon Exterior Angle Calculator",z.object({sides:n}),x=>2*Math.PI/x.sides,"beta=2pi/n","Each exterior angle of a regular n-gon in radians.",{sides:6},Math.PI/3);
export const regularPolygonDiagonalCount=mk("geometry.regular-polygon-diagonal-count","regular-polygon-diagonal-calculator","Regular Polygon Diagonal Count Calculator",z.object({sides:n}),x=>x.sides*(x.sides-3)/2,"D=n(n-3)/2","Number of diagonals in an n-sided polygon.",{sides:6},9);
export const regularPolygonApothem=mk("geometry.regular-polygon-apothem","regular-polygon-apothem-calculator","Regular Polygon Apothem Calculator",z.object({sideM:pos,sides:n}),x=>x.sideM/(2*Math.tan(Math.PI/x.sides)),"a=s/(2tan(pi/n))","Apothem of a regular polygon from side length and side count.",{sideM:2,sides:6},Math.sqrt(3));
export const regularPolygonCircumradius=mk("geometry.regular-polygon-circumradius","regular-polygon-circumradius-calculator","Regular Polygon Circumradius Calculator",z.object({sideM:pos,sides:n}),x=>x.sideM/(2*Math.sin(Math.PI/x.sides)),"R=s/(2sin(pi/n))","Circumradius of a regular polygon from side length and side count.",{sideM:2,sides:6},2);
const ann=z.object({outerRadiusM:pos,innerRadiusM:pos,centralAngleRad:z.number().finite().min(0).max(2*Math.PI)}).refine(x=>x.innerRadiusM<x.outerRadiusM,{message:"Inner radius must be smaller than outer radius"});
export const annularSectorArea=mk("geometry.annular-sector-area","annular-sector-area-calculator","Annular Sector Area Calculator",ann,x=>0.5*(x.outerRadiusM**2-x.innerRadiusM**2)*x.centralAngleRad,"A=(R^2-r^2)theta/2","Area of an annular sector from concentric radii and central angle in radians.",{outerRadiusM:4,innerRadiusM:2,centralAngleRad:Math.PI/2},3*Math.PI);
export const geometryBatch3Definitions=[regularPolygonExteriorAngle,regularPolygonDiagonalCount,regularPolygonApothem,regularPolygonCircumradius,annularSectorArea]as const;
