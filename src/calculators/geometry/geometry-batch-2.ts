import{z}from"zod";import type{CalculatorDefinition}from"../types";
type Out={value:number;steps:readonly string[]};const pos=z.number().finite().positive().max(1e50),nonneg=z.number().finite().min(0).max(1e50);
const src={label:"OpenStax — Plane geometry reference context",url:"https://openstax.org/details/books/prealgebra-2e",note:"Geometry reference context for circle, arc, chord, and regular-polygon relations."};
const checked=(v:number)=>{if(!Number.isFinite(v))throw new Error("Calculated result is outside the supported finite range");return v};const mk=<I>(id:string,slug:string,title:string,schema:z.ZodType<I>,calc:(x:I)=>number,expr:string,desc:string,example:I,expected:number)=>({id,slug,title,category:"geometry",version:1,riskClass:"standard",reviewStatus:"draft",inputSchema:schema,calculate:(x:I)=>{const value=checked(calc(x));return{value,steps:[title.replace(" Calculator","")+" = "+value]}},formulas:[{id,expression:expr,description:desc}],sources:[src],examples:[{label:"Worked example",input:example,expected:{value:expected,steps:[title.replace(" Calculator","")+" = "+expected]}}],goldenTests:[{label:"Worked example",input:example,expected:{value:expected}}]}) satisfies CalculatorDefinition<I,Out>;
export const chordLength=mk("geometry.circle-chord-length","circle-chord-length-calculator","Circle Chord Length Calculator",z.object({radiusM:pos,centralAngleRad:z.number().finite().min(0).max(Math.PI*2)}),x=>2*x.radiusM*Math.sin(x.centralAngleRad/2),"c=2r*sin(theta/2)","Length of a chord from circle radius and central angle in radians.",{radiusM:5,centralAngleRad:Math.PI/3},5);
export const arcLength=mk("geometry.circle-arc-length","circle-arc-length-calculator","Circle Arc Length Calculator",z.object({radiusM:pos,centralAngleRad:z.number().finite().min(0).max(Math.PI*2)}),x=>x.radiusM*x.centralAngleRad,"s=r*theta","Arc length for a central angle in radians.",{radiusM:5,centralAngleRad:Math.PI/2},2.5*Math.PI);
export const circularSegmentArea=mk("geometry.circular-segment-area","circle-segment-area-calculator","Circular Segment Area Calculator",z.object({radiusM:pos,centralAngleRad:z.number().finite().min(0).max(Math.PI*2)}),x=>0.5*x.radiusM*x.radiusM*(x.centralAngleRad-Math.sin(x.centralAngleRad)),"A=r^2(theta-sin(theta))/2","Minor/major circular segment area from radius and included central angle in radians.",{radiusM:2,centralAngleRad:Math.PI/2},Math.PI-2);
export const sectorPerimeter=mk("geometry.circular-sector-perimeter","circle-sector-perimeter-calculator","Circular Sector Perimeter Calculator",z.object({radiusM:pos,centralAngleRad:z.number().finite().min(0).max(Math.PI*2)}),x=>2*x.radiusM+x.radiusM*x.centralAngleRad,"P=2r+r*theta","Boundary length of a circular sector including both radii and its arc.",{radiusM:2,centralAngleRad:Math.PI/2},4+Math.PI);
export const regularPolygonInteriorAngle=mk("geometry.regular-polygon-interior-angle","regular-polygon-interior-angle-calculator","Regular Polygon Interior Angle Calculator",z.object({sides:z.number().int().min(3).max(1000000)}),x=>(x.sides-2)*Math.PI/x.sides,"alpha=(n-2)pi/n","Each interior angle of a regular n-gon in radians.",{sides:6},2*Math.PI/3);
export const geometryBatch2Definitions=[chordLength,arcLength,circularSegmentArea,sectorPerimeter,regularPolygonInteriorAngle]as const;
