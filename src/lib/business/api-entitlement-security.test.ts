import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/006_b8_api_automation.sql"),"utf8");const actions=readFileSync(join(process.cwd(),"src/app/business/api/actions.ts"),"utf8");
describe("Business API entitlement boundary",()=>{it("fails quota consumption closed after Business access ends",()=>{expect(sql).toContain("p.plan='business'")});it("requires Business before credential administration",()=>expect(actions).toContain('profile?.plan!=="business"'));it("does not silently clamp malformed quota settings",()=>{expect(actions).toContain("Number.isInteger(rateLimit)");expect(actions).toContain("Number.isInteger(monthlyQuota)")});});
