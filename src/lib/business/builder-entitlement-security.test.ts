import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/004_b6_builder.sql"),"utf8");
describe("Business builder entitlement boundary",()=>{it("requires Business inside security-definer builder RPCs",()=>{expect((sql.match(/p\.plan='business'/g)??[]).length).toBeGreaterThanOrEqual(2);expect((sql.match(/Business plan required/g)??[]).length).toBeGreaterThanOrEqual(2)});it("retains organization role gates",()=>{expect(sql).toContain("array['owner','admin','manager']")});});
