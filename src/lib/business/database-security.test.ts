import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/003_b5_business.sql"),"utf8");
describe("Business workspace database security",()=>{it("requires Business for organization creation",()=>expect(sql).toContain("plan = 'business'"));it("enforces organization membership RLS",()=>{expect(sql).toContain("organizations_member_select");expect(sql).toContain("projects_member_select");expect(sql).toContain("clients_member_select")});it("prevents accidental duplicate owner organizations",()=>expect(sql).toContain("Business account already owns an organization"));it("locks the five-seat acceptance ceiling",()=>expect(sql).toContain("member_count >= 5"));it("constrains persisted business text",()=>{expect(sql).toContain("char_length(description)<=2000");expect(sql).toContain("char_length(client_reference)<=240");expect(sql).toContain("char_length(calculator_slug) between 1 and 160")});});
