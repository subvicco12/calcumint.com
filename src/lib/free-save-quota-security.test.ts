import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/011_b8_free_save_quotas.sql"),"utf8");
describe("B8 Free save quota migration",()=>{it("protects both save tables",()=>{expect(sql).toContain("favorites_free_quota");expect(sql).toContain("calculation_history_free_quota")});it("locks the product limits",()=>{expect(sql).toContain("current_count>=10");expect(sql).toContain("current_count>=20")});it("exempts only paid plans",()=>expect(sql).toContain("account_plan not in ('pro','business')"));});
