import{readFileSync}from"node:fs";import{join}from"node:path";import{describe,expect,it}from"vitest";import{deterministicCalculatorSearch}from"./catalog";const sql=readFileSync(join(process.cwd(),"supabase/migrations/007_b9_ai.sql"),"utf8");
describe("AI governance",()=>{it("rejects invalid deterministic finder limits",()=>{expect(deterministicCalculatorSearch("loan",0)).toEqual([]);expect(deterministicCalculatorSearch("loan",11)).toEqual([]);expect(deterministicCalculatorSearch("loan",1.5)).toEqual([])});it("requires a Business builder role for AI builder access",()=>{expect(sql).toContain("p_feature='builder'");expect(sql).toContain("array['owner','admin','manager']");expect(sql).toContain("Builder permission required")});});
